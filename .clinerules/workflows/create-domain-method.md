---
name: create-domain-method
description: "Alias counterpart for the Cline workflow skill. Use the same instructions as `create-domain.md`."
---

# Workflow Alias: Create Domain (Flutter)

Use the canonical Cline workflow instructions in `.clinerules/workflows/create-domain.md`.
