# Deterministic Tooling Register

Canonical PACED register for deciding whether a skill's repeatable mechanics should stay in prose, move into lint/analyzer enforcement, or be extracted into deterministic tooling.

This register is advisory for skill maintenance. It does not authorize tool creation by itself. If a new canonical tool is added or materially changed, `tools/manifest.md` must still be updated in the same change.
It is intentionally an **extractability register**, not a rule-lifecycle, rule-metrics, or project-rule ledger.
PACED-level rules belong in `delphi-ai/`; PROJECT-level rules belong in the downstream repository and should be governed there by project constitution, modules, and any project-local deterministic enforcement.

Deterministic tooling extracted from this register should prefer diagnostic outputs that carry resolution instructions whenever the remedy is objective enough to state precisely. The goal is convergent enforcement, not opaque blocking.

## Classification Model
- `skill-only`
  - The skill is mainly governance, judgment, orchestration, or sequencing. Tooling may support prerequisites, but the core skill should remain prose-driven.
- `lint/analyzer`
  - The right deterministic extraction path is static analysis or rule enforcement, not a generic shell/python helper.
- `partial-tool`
  - Only the mechanical subset should be scripted. Human judgment still decides framing, tradeoffs, or interpretation.
- `full-tool-candidate`
  - The workflow is objective enough that a deterministic helper could plausibly execute most of it end-to-end.
- `already-backed`
  - A canonical tool/script already materially supports the repeatable mechanics of the skill.

## Review Heuristics
- Prefer `skill-only` for rules, governance methods, design exploration, and strategic transitions.
- Prefer `lint/analyzer` for static code-shape violations that should be prevented before runtime.
- Prefer `partial-tool` for audits, scanners, scaffolders, readiness checks, and report generation.
- Prefer `full-tool-candidate` only when inputs, checks, and outputs are objective enough to avoid fake determinism.
- Prefer `already-backed` when the repo already ships a canonical helper that materially carries the repetitive portion.

## Immediate Priorities
## Rule Skills

### Docker Rules
| Skill | Classification | Support / Preferred Shape |
| --- | --- | --- |
| `rule-docker-docker-architecture-mode-transition-model-decision` | `skill-only` | Governance trigger; keep as rule, not tool. |
| `rule-docker-docker-ci-pipeline-model-decision` | `skill-only` | Governance trigger; keep as rule, not tool. |
| `rule-docker-docker-runtime-ingress-model-decision` | `skill-only` | Governance trigger; keep as rule, not tool. |
| `rule-docker-documentation-migration-model-decision` | `skill-only` | Governance trigger; keep as rule, not tool. |
| `rule-docker-flutter-architecture` | `skill-only` | Governance trigger; keep as rule, not tool. |
| `rule-docker-shared-audit-escalation-model-decision` | `skill-only` | Governance trigger for deterministic audit-floor enforcement; the tool belongs to the workflow, not the rule itself. |
| `rule-docker-shared-core-instructions-always-on` | `skill-only` | Always-on baseline; deterministic tooling would be fake authority. |
| `rule-docker-shared-delphi-project-setup-model-decision` | `skill-only` | Setup trigger belongs in governance; setup reporting is backed by workflow helpers. The rule now points to `config/stack_capabilities.yaml` as available-capability context only, while project-active topology remains foundation/project-owned. |
| `rule-docker-shared-environment-topology-contract-model-decision` | `partial-tool` | Governance remains rule-level, but topology draft creation is supported by [`environment_topology_contract_scaffold.py`](../tools/environment_topology_contract_scaffold.py), which pre-fills project evidence, redacts secrets, and marks inferred runtime/domain/tenant/active-stack values for user validation. |
| `rule-docker-shared-foundation-docs-sync-model-decision` | `skill-only` | Canonical sync requirement; not a direct tool target. |
| `rule-docker-shared-initialization-readiness-model-decision` | `skill-only` | Governance shell around readiness checks; keep rule-level. Active stack/runtime facts are resolved from project-owned contracts, not from capability presence in Delphi. |
| `rule-docker-shared-project-mandate-always-on` | `skill-only` | Mandate authority must remain prose-driven. |
| `rule-docker-shared-realtime-delta-streams-model-decision` | `skill-only` | Architectural decision gate; not a deterministic helper target. |
| `rule-docker-shared-self-improvement-manual` | `skill-only` | Instruction-only session governance; not a deterministic helper target. |
| `rule-docker-shared-session-lifecycle-model-decision` | `skill-only` | Lifecycle governance; not a deterministic helper target. |
| `rule-docker-shared-todo-driven-execution-model-decision` | `partial-tool` | Tactical authority remains judgment-led, but authority/process evidence is supported by [`todo_authority_guard.py`](../tools/todo_authority_guard.py), which blocks missing approval evidence, missing touched-surface rule ingestion, missing delivery-gate rows under delivery claims, unresolved P1/P2 routing, and promotion findings that require split/renewed approval without a reference. Close-claim evidence remains supported by [`todo_completion_guard.py`](../tools/todo_completion_guard.py), which blocks delivery claims when DoD/Validation criteria lack row-level evidence, evidence is placeholder/aggregate-only, literal artifacts such as UI controls/routes/endpoints/schemas/runtime targets are not proven, visible/interactive criteria lack item-specific integration/device or navigation/browser evidence, browser/web-visible criteria lack source-owned Playwright spec + runner evidence when a Playwright suite exists, visible CRUD/mutation criteria lack local non-main mutation evidence, the Local CI-Equivalent Suite Matrix is missing/unpassed, or the Pipeline/Copilot P1/P2 Preflight / Rule-Spirit Anti-Pattern Hunt sections are missing, unexecuted, or record unresolved P1/P2 findings. |
| `rule-docker-shared-workflow-definition-model-decision` | `skill-only` | Workflow integrity rule; keep as governance. |

### Flutter Rules
| Skill | Classification | Support / Preferred Shape |
| --- | --- | --- |
| `rule-flutter-flutter-architecture-always-on` | `lint/analyzer` | Architecture enforcement should keep moving into analyzer rules, not shell helpers. |
| `rule-flutter-flutter-contract-alignment-always-on` | `skill-only` | Contract alignment still needs doc and code judgment. |
| `rule-flutter-flutter-controller-workflow-glob` | `skill-only` | File-path trigger; deterministic value lives in the target workflow, not the glob rule. |
| `rule-flutter-flutter-documentation-contracts-always-on` | `skill-only` | Canonical doc sync is judgment-heavy even when checks assist. |
| `rule-flutter-flutter-domain-workflow-glob` | `skill-only` | File-path trigger; deterministic value lives in the target workflow, not the glob rule. |
| `rule-flutter-flutter-repository-workflow-glob` | `skill-only` | File-path trigger; branch-delta checks can support but not replace the rule. |
| `rule-flutter-flutter-route-workflow-glob` | `skill-only` | File-path trigger; route audit support belongs in the workflow/tooling, not this rule. |
| `rule-flutter-flutter-screen-workflow-glob` | `skill-only` | File-path trigger; keep as routing rule into the right skill/workflow. |

### Laravel Rules
| Skill | Classification | Support / Preferred Shape |
| --- | --- | --- |
| `rule-laravel-shared-ability-catalog-sync-model-decision` | `skill-only` | Catalog sync is governance-first; static checks can support later. |
| `rule-laravel-shared-core-instructions-always-on` | `skill-only` | Always-on baseline; deterministic tooling would be fake authority. |
| `rule-laravel-shared-domain-resolution-testing-model-decision` | `skill-only` | Decision trigger belongs in governance; route/test scanning belongs in workflow support. |
| `rule-laravel-shared-foundation-docs-sync-model-decision` | `skill-only` | Canonical sync requirement; not a direct tool target. |
| `rule-laravel-shared-initialization-readiness-model-decision` | `skill-only` | Governance shell around readiness checks; keep rule-level. |
| `rule-laravel-shared-project-mandate-always-on` | `skill-only` | Mandate authority must remain prose-driven. |
| `rule-laravel-shared-realtime-delta-streams-model-decision` | `skill-only` | Architectural decision gate; not a deterministic helper target. |
| `rule-laravel-shared-self-improvement-manual` | `skill-only` | Instruction-only session governance; not a deterministic helper target. |
| `rule-laravel-shared-session-lifecycle-model-decision` | `skill-only` | Lifecycle governance; not a deterministic helper target. |
| `rule-laravel-shared-settings-kernel-patch-contract-model-decision` | `skill-only` | Contract policy remains judgment-heavy even if future validators help. |
| `rule-laravel-shared-tenant-access-guardrails-model-decision` | `skill-only` | Decision trigger belongs in governance; scanning belongs in workflow support. |
| `rule-laravel-shared-todo-driven-execution-model-decision` | `partial-tool` | Tactical authority remains judgment-led, but authority/process evidence is supported by [`todo_authority_guard.py`](../tools/todo_authority_guard.py), which blocks missing approval evidence, missing touched-surface rule ingestion, missing delivery-gate rows under delivery claims, unresolved P1/P2 routing, and promotion findings that require split/renewed approval without a reference. Close-claim evidence remains supported by [`todo_completion_guard.py`](../tools/todo_completion_guard.py), which blocks delivery claims when DoD/Validation criteria lack row-level evidence, evidence is placeholder/aggregate-only, literal artifacts such as UI controls/routes/endpoints/schemas/runtime targets are not proven, visible/interactive criteria lack item-specific integration/device or navigation/browser evidence, browser/web-visible criteria lack source-owned Playwright spec + runner evidence when a Playwright suite exists, visible CRUD/mutation criteria lack local non-main mutation evidence, the Local CI-Equivalent Suite Matrix is missing/unpassed, or the Pipeline/Copilot P1/P2 Preflight / Rule-Spirit Anti-Pattern Hunt sections are missing, unexecuted, or record unresolved P1/P2 findings. |
| `rule-laravel-shared-workflow-definition-model-decision` | `skill-only` | Workflow integrity rule; keep as governance. |

## Flutter Umbrella and Smell Skills
| Skill | Classification | Support / Preferred Shape |
| --- | --- | --- |
| `flutter-architecture-adherence` | `lint/analyzer` | Prefer analyzer/lint expansion through the global analyzer plugin, with [`rule_spirit_anti_pattern_scan.sh`](../tools/rule_spirit_anti_pattern_scan.sh) as delivery-gate support for bypass-shape review and project-owned analyzer reset scripts for stale analyzer state. |
| `flutter-performance-smell-scanner` | `skill-only` | Umbrella orchestration skill; deterministic support belongs in the smell-specific rules or analyzers. |
| `flutter-smell-async-navigation` | `lint/analyzer` | Best prevented by analyzer rules for async navigation ownership. |
| `flutter-smell-build-side-effects` | `lint/analyzer` | Best prevented by analyzer rules for side effects in widget lifecycle/build. |
| `flutter-smell-image-media` | `lint/analyzer` | Static heuristics are a better fit than a standalone shell tool. |
| `flutter-smell-layout-hotspots` | `lint/analyzer` | Static heuristics are a better fit than a standalone shell tool. |
| `flutter-smell-list-performance` | `lint/analyzer` | Static heuristics are a better fit than a standalone shell tool. |
| `flutter-smell-mounted-checks` | `lint/analyzer` | Analyzer/lint route is stronger than shell scripting for `mounted` misuse. |
| `flutter-widget-local-state-heuristics` | `lint/analyzer` | Widget-state boundary is primarily static-shape governance. |

## Operational and Audit Skills
| Skill | Classification | Support / Preferred Shape |
| --- | --- | --- |
| `branch-rebaseline-preflight` | `already-backed` | Existing support via [`branch_rebaseline_preflight.sh`](../tools/branch_rebaseline_preflight.sh); use the helper as the default audit path, require ancestry + patch-equivalence false-positive detection against `origin/dev`, keep remote deletion manual, and keep gitlink ownership out of this workflow. |
| `backend-concurrency-idempotency-validation` | `already-backed` | Existing support via [`backend_concurrency_probe.sh`](../tools/backend_concurrency_probe.sh); this is the strongest of the new helpers because it runs real `5|10|20`-style concurrent HTTP probes. Human judgment still validates domain invariants and idempotency policy. |
| `bug-fix-evidence-loop` | `already-backed` | Existing support via [`bug_fix_evidence_scaffold.sh`](../tools/bug_fix_evidence_scaffold.sh); use it to structure reproduction, mandatory questions, and stage coverage before diagnosis. |
| `audit-protocol-triple-review` | `already-backed` | Existing support via [`triple_audit_session.py`](audit-protocol-triple-review/scripts/triple_audit_session.py) plus [`subagent_review_dispatch.py`](../tools/subagent_review_dispatch.py) and [`subagent_review_merge.py`](../tools/subagent_review_merge.py); use the session runner to create/resume round state, validate result files, classify `clean|needs_resolution|needs_adjudication`, block `next-round` until non-clean rounds have recorded `resolution.md` status `resolved|accepted-debt`, generate per-round effective packages that include prior resolutions, and inject lane-specific blocker calibration into dispatch packages. The skill now requires a `Frontend / Consumer Matrix` in triggered producer-surface packages, but matrix completeness remains a package-preparation judgment until deterministic TODO validation grows a dedicated check. Adjudication judgment remains in the skill; the script enforces state completeness and no-context decision carry-forward while Delphi classifies marginal findings as accepted debt instead of extending the loop. |
| `endpoint-performance-scrutiny` | `partial-tool` | Existing support via [`endpoint_performance_review_scaffold.sh`](../tools/endpoint_performance_review_scaffold.sh) plus [`exact_lookup_anti_pattern_audit.sh`](../tools/exact_lookup_anti_pattern_audit.sh), but the current tooling is still scaffold + heuristic scan. Stronger proof still depends on `explain`, query logs, benchmarks, or equivalent runtime evidence. |
| `frontend-race-condition-validation` | `already-backed` | Existing support via [`frontend_race_probe.sh`](../tools/frontend_race_probe.sh) plus [`frontend_race_validation_scaffold.sh`](../tools/frontend_race_validation_scaffold.sh); the probe runs real burst/repetition orchestration while scenario semantics remain in stack-native tests. |
| `ci-equivalent-test-surface-admission` | `skill-only` | Canonical admission gate for adding or rewiring stage-facing tests, wrappers, lifecycle steps, and readonly/mutation coverage without local/pipeline drift. The core judgment remains prose-driven. Existing audits such as [`ci_pipeline_surface_audit.sh`](../tools/ci_pipeline_surface_audit.sh), [`test_orchestration_status_report.sh`](../tools/test_orchestration_status_report.sh), and downstream fail-closed drift guards support evidence collection but do not replace the admission decision. |
| `ci-equivalent-governance` | `partial-tool` | Canonical operator-facing definition of `CI Equivalent`, broad local stage-gate naming, lifecycle-step completeness, and authoritative-branch proof. Mechanical support comes from [`test_orchestration_status_report.sh`](../tools/test_orchestration_status_report.sh), [`ci_pipeline_surface_audit.sh`](../tools/ci_pipeline_surface_audit.sh), [`todo_authority_guard.py`](../tools/todo_authority_guard.py), [`todo_completion_guard.py`](../tools/todo_completion_guard.py), and [`orchestration_reconcile_replay_guard.py`](../tools/orchestration_reconcile_replay_guard.py), but final scope/parity judgment remains human-led. |
| `claude-cli-calling` | `already-backed` | Existing support via [`run_claude_print.sh`](claude-cli-calling/scripts/run_claude_print.sh); use it to enforce stdin-based Claude CLI invocation, bounded tool access, compact-packet fallback, and stream-json liveness diagnostics with `--include-partial-messages` plus `--verbose` when `--print` is active. The runner auto-enables `--verbose` for `stream-json` so the canonical diagnostic path stays valid. The current auth-mode caveat still applies: `--bare` disables OAuth/keychain reads and therefore requires API-key auth instead of local Claude login state. For local-inspection audits, the canonical invocation must keep a minimum read-only tool set instead of going fully toolless, otherwise pseudo-command loops can replace actionable findings. |
| `copilot-pr-review` | `already-backed` | Existing support via [`build_copilot_review_packet.sh`](copilot-pr-review/scripts/build_copilot_review_packet.sh), [`run_claude_review_pass.sh`](copilot-pr-review/scripts/run_claude_review_pass.sh), [`finding_carry_forward_extract.py`](../tools/finding_carry_forward_extract.py), `review-finding-classification`, `claude-cli-calling`, [`test_orchestration_status_report.sh`](../tools/test_orchestration_status_report.sh), and the no-context packet helpers [`subagent_review_dispatch.py`](../tools/subagent_review_dispatch.py) and [`subagent_review_merge.py`](../tools/subagent_review_merge.py). The mandated order is internal no-context subagent sweep first and Claude/Copilot-sim second; tooling now carries prior TODO dispositions into the bounded packet so repeated by-design/resolved findings are not rediscovered as fresh blockers, while final finding acceptance and TODO-contract comparison remain judgment-led. The skill also requires commit-based baseline resets: once a remediation wave is accepted and validated, commit it on the active review branch and rebuild the packet before the next review pass so the loop stays anchored to the new `base...HEAD` instead of an aging worktree snapshot. For promotion/readiness flows, the active review branch may be a derived remediation branch that preserves the promotable source-branch history until the accepted net effect is replayed back as curated commit(s). CI-equivalent semantics, broad stage-gate naming, and reconcile-wrapper exceptions now delegate to `ci-equivalent-governance`, while stage-facing test/harness rewires route through `ci-equivalent-test-surface-admission` before renewed readiness claims. |
| `review-finding-classification` | `partial-tool` | Canonical operator-facing post-review triage for `Copilot real|Copilot mimic|Codex|Claude|no-context|CI|promotion` findings. Use [`review_finding_routing_scaffold.py`](../tools/review_finding_routing_scaffold.py) only to render guard-compatible `Promotion Finding Routing Ledger` rows/sections from operator-supplied metadata, and use [`finding_carry_forward_extract.py`](../tools/finding_carry_forward_extract.py) to carry prior authoritative dispositions forward into later review loops. The helper support is intentionally mechanical only; blocker-vs-follow-up judgment remains with the operator against the governing TODO, taxonomy, and routing canon. |
| `flutter-device-test-runner` | `already-backed` | Existing support via [`device_single_test_resilient.sh`](flutter-device-test-runner/scripts/device_single_test_resilient.sh); future work is report ergonomics, not full replacement. |
| `github-stage-promotion-orchestrator` | `already-backed` | Umbrella router for manual `dev-only|through-stage` promotion. Phase details now live in `github-stage-promotion-*` phase skills for progressive disclosure. Existing support via [`github_promotion_contract_init.sh`](../tools/github_promotion_contract_init.sh), [`github_stage_promotion_scenario_classifier.py`](../tools/github_stage_promotion_scenario_classifier.py), [`github_promotion_diff_guard.sh`](../tools/github_promotion_diff_guard.sh), [`github_promotion_action_guard.sh`](../tools/github_promotion_action_guard.sh), guarded mutation wrappers, [`github_promotion_source_authority_guard.py`](../tools/github_promotion_source_authority_guard.py) for governing-TODO branch/sha authority validation, [`github_stage_promotion_preflight.sh`](../tools/github_stage_promotion_preflight.sh), [`orchestration_reconcile_replay_guard.py`](../tools/orchestration_reconcile_replay_guard.py) for reconcile-origin handoff proof, [`github_stage_promotion_snapshot.sh`](../tools/github_stage_promotion_snapshot.sh), [`github_status_wait.py`](../tools/github_status_wait.py) for low-noise Actions/branch waits, [`github_promotion_completion_guard.sh`](../tools/github_promotion_completion_guard.sh), `copilot-pr-review`, `review-finding-classification`, `ci-equivalent-test-surface-admission`, [`finding_carry_forward_extract.py`](../tools/finding_carry_forward_extract.py), [`subagent_review_dispatch.py`](../tools/subagent_review_dispatch.py), [`subagent_review_merge.py`](../tools/subagent_review_merge.py), [`todo_authority_guard.py`](../tools/todo_authority_guard.py), and [`test_orchestration_status_report.sh`](../tools/test_orchestration_status_report.sh) for TODO-native promotion finding routing; the mandated review order is internal no-context subagents before Claude/Copilot-sim, prior TODO dispositions now travel with the review packet so repeated by-design/resolved findings do not trigger endless fix-revert loops, and accepted remediation waves must be committed before the next promotion review pass so the lane stays on a stable diff baseline. When the lane wants clean promotable history, those iterative commits now live on a derived remediation branch and only the accepted net effect is replayed onto the authoritative source branch before PR creation. If that accepted state was first integrated on an orchestration-only `reconcile/*` branch, promotion resumes only after replay onto the canonical version/source branch and a green replay guard; reconcile itself is never the promotable PR source. CI-equivalent semantics, broad stage-gate naming, and reconcile-wrapper exceptions now delegate to `ci-equivalent-governance`, while stage-facing test/harness rewires must pass through `ci-equivalent-test-surface-admission` before renewed lane-readiness claims. The skill now explicitly forbids using a fresh Docker/root promotion branch as a substitute for repo-local Flutter/Laravel source authority and forbids root-level gitlink reset/normalization commits that push accepted app SHAs back toward unrelated `dev`/`stage` baselines. For package/version lanes, the source branch must also match the governing TODO's `Current Branch Authority` and exact recorded `branch@sha` baseline before preflight is considered green. The guards still distinguish narrow CI test-harness workflow adjustments from broader CI control-plane changes instead of treating them as one escape hatch, while also blocking `bot/next-version -> stage`, generated `web-app` PR mutation, promotion attempts from `reconcile/*`, and unresolved P1/P2 routing. For `dev-only`, stop at healthy requested source repo(s) on `dev` unless Docker finalization was explicitly included; for `through-stage`, require Docker finalization and the stage completion guard before claiming completion. |
| `github-stage-promotion-intake-classification` | `partial-tool` | Explicit authorization and scope remain human/manual, while repeatable source/base scenario classification is supported by [`github_stage_promotion_scenario_classifier.py`](../tools/github_stage_promotion_scenario_classifier.py). Treat classifier output as advisory evidence, not promotion authorization. |
| `github-stage-promotion-contract-preflight` | `already-backed` | Phase skill supported by contract init, source preflight, action/diff guards, guarded wrappers, [`orchestration_reconcile_replay_guard.py`](../tools/orchestration_reconcile_replay_guard.py) for reconcile-origin promotion handoff, `copilot-pr-review`, `review-finding-classification`, `ci-equivalent-test-surface-admission`, and the no-context packet helpers [`subagent_review_dispatch.py`](../tools/subagent_review_dispatch.py) and [`subagent_review_merge.py`](../tools/subagent_review_merge.py) before the first PR. The mandated preflight order is internal no-context subagent sweep first and Claude/Copilot-style confirmation second; TODO-contract comparison and final disposition remain manual. CI-equivalent semantics and stage-parity sufficiency now delegate to `ci-equivalent-governance`, while stage-facing test/harness rewires must complete the admission step before pre-promotion readiness is claimed. |
| `github-stage-promotion-source-to-dev` | `already-backed` | Phase skill for normal source PR movement into `dev`, supported by guarded commit/push/PR wrappers, source preflight, snapshots, and Copilot/check review. |
| `github-stage-promotion-bot-next-version-recovery` | `already-backed` | Phase skill for lane-owned Docker gitlink recovery and `bot/next-version -> dev`; supported by source preflight and updated action/diff guards that forbid direct `bot/next-version -> stage`. |
| `github-stage-promotion-dev-to-stage` | `already-backed` | Phase skill for `dev -> stage` lane-to-lane PR movement after source and gitlink work is healthy on `dev`; supported by guarded PR wrappers and snapshot/check evidence. |
| `github-stage-promotion-docker-finalization` | `already-backed` | Phase skill for app through-stage Docker gitlink follow-through; supported by bot recovery, stage promotion preflight, and `github_promotion_completion_guard.sh`. Docker finalization is limited to carrying the accepted authoritative app SHAs forward and explicitly must not substitute for app-source authority or rebaseline gitlinks back to stale Docker-root baselines. |
| `github-stage-promotion-failure-review` | `partial-tool` | Failure triage and root-cause classification remain judgment-led; snapshots, local CI-equivalent commands, Copilot evidence, `copilot-pr-review`, [`finding_carry_forward_extract.py`](../tools/finding_carry_forward_extract.py), the no-context packet helpers [`subagent_review_dispatch.py`](../tools/subagent_review_dispatch.py) and [`subagent_review_merge.py`](../tools/subagent_review_merge.py), and independent critique can support but not replace the finding decision. When findings are recorded, [`todo_authority_guard.py`](../tools/todo_authority_guard.py) validates that unresolved P1/P2 findings block completion while same-scope remediation can stay inside the same governing TODO/lane, and the operator must still compare each finding against the approved TODO decisions plus prior adjudicated dispositions. |
| `github-stage-promotion-closeout-report` | `already-backed` | Phase skill for dev-only or through-stage completion reporting, TODO closeout status, authority-guard routing evidence, and completion guard evidence when stage is in scope. |
| `github-main-promotion-orchestrator` | `already-backed` | Existing support via [`github_stage_promotion_snapshot.sh`](../tools/github_stage_promotion_snapshot.sh), [`github_status_wait.py`](../tools/github_status_wait.py) for low-noise Actions/branch waits, [`github_promotion_completion_guard.sh`](../tools/github_promotion_completion_guard.sh), [`orchestration_reconcile_replay_guard.py`](../tools/orchestration_reconcile_replay_guard.py) as carried-forward evidence when the lane originated on reconcile, `copilot-pr-review`, `review-finding-classification`, `ci-equivalent-test-surface-admission`, [`finding_carry_forward_extract.py`](../tools/finding_carry_forward_extract.py), [`test_orchestration_status_report.sh`](../tools/test_orchestration_status_report.sh), and the no-context packet helpers [`subagent_review_dispatch.py`](../tools/subagent_review_dispatch.py) and [`subagent_review_merge.py`](../tools/subagent_review_merge.py); use the snapshot to collect the current branch/PR/check evidence, require the internal no-context subagent sweep before the Claude/Copilot-style confirmation after preflight readiness and before the first PR, carry prior TODO dispositions into the review packet, and, when the lane wants clean promotable history, use a derived remediation branch for the iterative review-loop commits before replaying the accepted net effect onto the authoritative source branch. The authoritative source branch must already be CI-equivalent green before review opens, the review branch must pass its own CI-equivalent gate before it can be declared clean, and any replay that changes the authoritative source codebase forces a renewed authoritative CI-equivalent pass. CI-equivalent semantics, broad stage-gate naming, and reconcile-wrapper exceptions now delegate to `ci-equivalent-governance`, while stage-facing test/harness rewires must pass through `ci-equivalent-test-surface-admission` before renewed readiness claims. Then require the completion guard before claiming completion so Docker finalization and Flutter web follow-through are validated deterministically instead of assumed. Finding acceptance remains TODO-contract and risk judgment, not tool authority. |
| `runtime-load-stress-validation` | `already-backed` | Existing support via [`runtime_load_probe.sh`](../tools/runtime_load_probe.sh) plus [`runtime_load_validation_scaffold.sh`](../tools/runtime_load_validation_scaffold.sh); the probe runs deterministic staged HTTP load/stress checks with threshold evaluation while non-HTTP paths may still need stack-local harnesses. |
| `security-adversarial-review` | `skill-only` | Threat modeling and exploitability judgment should stay human; helpers may collect evidence only. |
| `stitch-mcp-design-workflow` | `skill-only` | The MCP already provides the deterministic tool layer; the skill is safe sequencing and fallback strategy. |
| `test-creation-standard` | `already-backed` | Existing support via [`test_coverage_matrix_scaffold.sh`](../tools/test_coverage_matrix_scaffold.sh); use it to scaffold coverage/gate planning before choosing real tests. When a new or changed test also changes a stage-facing suite family, wrapper, lifecycle step, or readonly/mutation row, route admission through `ci-equivalent-test-surface-admission` instead of relying on memory. |
| `test-orchestration-suite` | `already-backed` | Existing support via [`test_orchestration_status_report.sh`](../tools/test_orchestration_status_report.sh); use it to enforce explicit required-stage accounting while keeping suite selection, failure classification, and waiver judgment human. Runtime/domain ambiguity should route through [`environment_topology_contract_scaffold.py`](../tools/environment_topology_contract_scaffold.py) before browser/device/build evidence is trusted. CI-equivalent semantics, broad stage-gate naming, and reconcile-wrapper exceptions now delegate to `ci-equivalent-governance`; stage-facing suite/wrapper rewires now route through `ci-equivalent-test-surface-admission`; this skill stays responsible for suite selection, execution order, and stage accounting. |
| `test-quality-audit` | `already-backed` | Existing support via [`test_quality_audit.sh`](../tools/test_quality_audit.sh); use it for deterministic static findings and keep the final audit judgment in the skill. |
| `verification-debt-audit` | `already-backed` | Existing support via [`verification_debt_audit.sh`](../tools/verification_debt_audit.sh); use it for deterministic evidence collection and keep final closure judgment in the skill. |

## Docker Workflow Skills
| Skill | Classification | Support / Preferred Shape |
| --- | --- | --- |
| `wf-docker-architecture-mode-transition-method` | `skill-only` | Strategic transition workflow; keep judgment-driven. |
| `wf-docker-audit-escalation-method` | `already-backed` | Existing support via [`audit_escalation_guard.py`](../tools/audit_escalation_guard.py); require the canonical `Audit Trigger Matrix`, emit TEACH guidance for critique/final/test/security/performance/debt lanes, and rerun whenever trigger fields change materially. |
| `wf-docker-brownfield-normalization-method` | `already-backed` | Existing support via [`project_setup_normalization_packet.py`](../tools/project_setup_normalization_packet.py) plus [`project_recalibration_doctor.sh`](../tools/project_recalibration_doctor.sh); use them to separate manual-remediation versus normalization-TODO tracks from a derived setup report before opening remediation work. |
| `wf-docker-delphi-project-setup-method` | `already-backed` | Existing support via [`delphi_project_setup_report.sh`](../tools/delphi_project_setup_report.sh), [`project_setup_normalization_packet.py`](../tools/project_setup_normalization_packet.py), and [`project_recalibration_doctor.sh`](../tools/project_recalibration_doctor.sh); use them to collect lane/readiness/drift evidence and derive bounded normalization tracks. The concise skill delegates normative behavior to the workflow and `config/stack_capabilities.yaml` to reduce mirror drift. |
| `wf-docker-documentation-migration-method` | `skill-only` | Migration/gap analysis is too judgment-heavy for deterministic extraction. |
| `wf-docker-deterministic-todo-validation-method` | `already-backed` | Existing support via [`todo_validation_bundle_export.py`](../tools/todo_validation_bundle_export.py) and [`todo_deterministic_validator.py`](../tools/todo_deterministic_validator.py); use them to block missing structural TODO obligations with diagnostic output. |
| `wf-docker-environment-readiness-method` | `already-backed` | Existing support via [`environment_readiness_report.sh`](../tools/environment_readiness_report.sh), [`environment_topology_contract_scaffold.py`](../tools/environment_topology_contract_scaffold.py), [`verify_context.sh`](../tools/verify_context.sh), [`verify_adherence_sync.sh`](../tools/verify_adherence_sync.sh), and [`verify_environment.sh`](../scripts/docker/verify_environment.sh). The skill is intentionally concise and delegates stack activation/topology rules to canonical workflow/config surfaces. |
| `wf-docker-environment-topology-contract-method` | `already-backed` | Existing support via [`environment_topology_contract_scaffold.py`](../tools/environment_topology_contract_scaffold.py); use it to create redacted drafts from available downstream evidence while user validation remains mandatory for inferred topology facts. |
| `wf-docker-genesis-bootstrap-method` | `skill-only` | Bootstrap interviews and canonicalization remain human-led. |
| `wf-docker-independent-critique-method` | `partial-tool` | Independent no-context critique remains reviewer-driven, while [`audit_escalation_guard.py`](../tools/audit_escalation_guard.py) decides when the lane is required and how aggressive the package depth must be, and [`review_scope_drift_guard.py`](../tools/review_scope_drift_guard.py) now compares the post-review TODO against the pushed pre-review baseline so material scope drift forces renewed user validation before approval resumes. |
| `wf-docker-independent-final-review-method` | `skill-only` | Independent no-context final review remains reviewer-driven; [`audit_escalation_guard.py`](../tools/audit_escalation_guard.py) now decides the minimum delivery-side review floor and depth. |
| `wf-docker-independent-test-quality-audit-method` | `skill-only` | Independent test-quality audit remains reviewer-driven; [`audit_escalation_guard.py`](../tools/audit_escalation_guard.py) decides when the lane is required, while `test-quality-audit` provides the audit lens. |
| `wf-docker-persona-selection-method` | `skill-only` | Profile/persona choice is governance, not deterministic tooling. |
| `wf-docker-performance-concurrency-validation-method` | `skill-only` | This workflow is the canonical `pcv-1` policy package. Deterministic tooling belongs in the lane-specific helpers, not in faux-deterministic policy automation. |
| `wf-docker-post-session-review-method` | `skill-only` | Principle extraction and English review remain human tasks; automation should stay assistive only. |
| `wf-docker-progressive-determinism-metrics-method` | `already-backed` | Existing support via [`seed_rule_catalog.py`](../tools/seed_rule_catalog.py), [`rule_event_record.py`](../tools/rule_event_record.py), [`gate_finding_resolution_extract.py`](../tools/gate_finding_resolution_extract.py), [`gate_finding_resolution_scaffold.py`](../tools/gate_finding_resolution_scaffold.py), and [`paced_metrics_summary.py`](../tools/paced_metrics_summary.py); use them to seed teaching-rule metadata, log episodes, record `gate-escape` events for CI/Copilot P1/P2 and Rule-Spirit escapes, extract TODO-native gate resolutions, and derive clean-rate summaries without central manual counters. |
| `wf-docker-effort-selection-method` | `partial-tool` | Canonical effort/GOAL matrix remains workflow-led, while [`effort_selection_advisor.py`](../tools/effort_selection_advisor.py) provides a small advisory helper for mapping a declared surface plus material-ambiguity/goals-supported flags into a recommended effort tier and GOAL policy. The helper is intentionally non-blocking: it suggests `medium` for routine execution and executor subagents, the highest review-focused tier for formal review surfaces, and GOAL requirements for executor subagents when supported, but final classification remains operator judgment. |
| `wf-docker-profile-selection-method` | `skill-only` | Profile selection is governance, not deterministic tooling. |
| `wf-docker-runtime-index-method` | `already-backed` | Existing support via [`runtime_session_index.py`](../tools/runtime_session_index.py); use it to surface active TODO continuity, blockers, and open handoffs without creating a new source of truth. |
| `wf-docker-realtime-delta-streams-method` | `skill-only` | SSE contract design remains architectural judgment. |
| `wf-docker-self-improvement-session-method` | `skill-only` | Instruction-only session governance should stay human-led. |
| `wf-docker-session-lifecycle-method` | `skill-only` | Lifecycle governance should stay human-led. |
| `wf-docker-subagent-orchestration-method` | `already-backed` | Existing support via [`subagent_review_dispatch.py`](../tools/subagent_review_dispatch.py), [`subagent_review_merge.py`](../tools/subagent_review_merge.py), [`finding_carry_forward_extract.py`](../tools/finding_carry_forward_extract.py), and `review-finding-classification`; use them to package bounded no-context review requests, carry prior TODO dispositions into the dispatch, merge structured reviewer output, and then route deduplicated review findings through the canonical classification skill instead of inventing hidden authority. Producer-surface packages must include the TODO `Frontend / Consumer Matrix` before dispatch; deterministic helpers carry the packet, while the skill owns the triggered-matrix judgment. |
| `wf-docker-subagent-worktree-reconciliation-method` | `partial-tool` | Existing support via [`orchestration_plan_completion_guard.py`](../tools/orchestration_plan_completion_guard.py), which blocks incomplete orchestration plans before approval/execution claims, including missing acceptance traceability, untracked DoD/validation criteria, missing CI-equivalent local suite matrices, unapproved spec-marker substitutions, missing authoritative return-branch / reconcile-failure-routing / post-reconcile promotion-source declarations, and orchestrator-owned implementation rows; [`orchestration_delivery_guard.py`](../tools/orchestration_delivery_guard.py), which blocks delivery claims when traceability evidence is incomplete, runtime/browser/device freshness is unproven, named artifacts lack matching evidence, validation rows are missing, the CI-equivalent local suite matrix is absent/unpassed, or the orchestrator is listed as implementation owner; [`orchestration_reconcile_replay_guard.py`](../tools/orchestration_reconcile_replay_guard.py), which blocks promotion or non-orchestration closeout from resuming until the accepted reconcile state is explicitly replayed onto the canonical branch with deterministic git/topology evidence; [`scripts/docker/delphi/run_reconcile_validation.sh`](../scripts/docker/delphi/run_reconcile_validation.sh), which enforces principal-checkout `reconcile/*` branch discipline for configured source checkouts during authoritative local consolidated validation and offers project-configurable repo commands for stack-specific lanes such as Laravel, Flutter, or Go; and [`scripts/docker/delphi/run_navigation_reconcile_validation.sh`](../scripts/docker/delphi/run_navigation_reconcile_validation.sh), which blocks local browser validation until the authoritative source checkouts are on `reconcile/*`, configured live Docker bind mounts resolve to the principal checkout, and the required navigation env is already present or sourced from the local navigation env file. Worker checkpoint acceptance now explicitly requires clean analyzer/lint, targeted test, and applicable build/publish evidence for the owned slice, while CI-equivalent semantics, broad stage-gate naming, and reconcile-wrapper exceptions now delegate to `ci-equivalent-governance`. Worker GOAL contracts remain orchestration judgment, and effort-tier selection now routes through `wf-docker-effort-selection-method`; the guards validate ownership/evidence shape, but not whether the chosen executor goal or effort level is good. Checkpoint-manifest validation, branch/worktree scaffolding, and final merge strategy still require orchestrator judgment. |
| `wf-docker-todo-driven-execution-method` | `partial-tool` | Umbrella state machine for TODO-driven execution. Phase details now live in the `wf-docker-todo-*` phase skills/workflows for progressive disclosure, while approval/rule-ingestion and delivery-gate routing are supported by [`todo_authority_guard.py`](../tools/todo_authority_guard.py), delivery closure remains supported by [`todo_completion_guard.py`](../tools/todo_completion_guard.py), canonical post-review triage is routed through `review-finding-classification`, pre-approval drift control is now backed by [`review_scope_drift_guard.py`](../tools/review_scope_drift_guard.py), and bypass-shape review is supported by [`rule_spirit_anti_pattern_scan.sh`](../tools/rule_spirit_anti_pattern_scan.sh) with severity triage, temporary allowlists, and optional JSON artifacts. |
| `wf-docker-todo-lane-framing-method` | `skill-only` | Phase skill for lane classification and feature-framing decisions. Tooling support is indirect through TODO/feature-brief templates; final lane choice remains judgment-led. |
| `wf-docker-todo-contract-refinement-method` | `skill-only` | Phase skill for anchors, decisions, assumptions, complexity, and planning matrices. Deterministic checks can validate close claims later, but refinement quality remains judgment-led. |
| `wf-docker-todo-approval-gates-method` | `already-backed` | Phase skill for plan review and approval gates, supported by [`audit_escalation_guard.py`](../tools/audit_escalation_guard.py), [`subagent_review_dispatch.py`](../tools/subagent_review_dispatch.py), [`subagent_review_merge.py`](../tools/subagent_review_merge.py), `audit-protocol-triple-review` when the floor requires it, [`review_scope_drift_guard.py`](../tools/review_scope_drift_guard.py) for post-review baseline-vs-current comparison, and [`todo_authority_guard.py`](../tools/todo_authority_guard.py) after approval is recorded. Effort-tier selection now routes through `wf-docker-effort-selection-method`: helpers validate evidence/routing, not whether the model was run at the correct review-focused effort. |
| `wf-docker-todo-execution-boundary-method` | `partial-tool` | Phase skill for approved-boundary execution and scope-change control. Boundary judgment remains human-led, while [`todo_authority_guard.py`](../tools/todo_authority_guard.py) blocks missing approval/rule-ingestion evidence before implementation proceeds. |
| `wf-docker-todo-delivery-gates-method` | `partial-tool` | Phase skill for delivery evidence, CI-equivalent execution, P1/P2 preflight, Rule-Spirit hunt, audits, and final guard. Supported by [`todo_authority_guard.py`](../tools/todo_authority_guard.py), [`todo_completion_guard.py`](../tools/todo_completion_guard.py), `review-finding-classification` plus [`review_finding_routing_scaffold.py`](../tools/review_finding_routing_scaffold.py) for guard-compatible routing scaffolds, [`rule_spirit_anti_pattern_scan.sh`](../tools/rule_spirit_anti_pattern_scan.sh) for severity-aware heuristic scans and JSON evidence, and audit/test/debt helpers. CI-equivalent semantics, broad stage-gate naming, and reconcile-wrapper exceptions now delegate to `ci-equivalent-governance`; any delivery change that rewires the stage-facing suite family now routes through `ci-equivalent-test-surface-admission`; this skill stays responsible for execution evidence, guard routing, close-claim discipline, while effort-tier selection for orchestrator-side delivery/final-review now routes through `wf-docker-effort-selection-method`. |
| `wf-docker-todo-closeout-promotion-method` | `partial-tool` | Phase skill for blocked-state, canonicalization, promotion-lane, and closeout decisions. Close-claim moves are supported by [`todo_authority_guard.py`](../tools/todo_authority_guard.py), [`todo_completion_guard.py`](../tools/todo_completion_guard.py), and [`todo_closeout_guard.py`](../tools/todo_closeout_guard.py), which blocks delivered TODOs from remaining in `active/` without a valid disposition and actionable next step; reconcile-origin promotion handoff is additionally supported by [`orchestration_reconcile_replay_guard.py`](../tools/orchestration_reconcile_replay_guard.py), while promotion-specific deterministic support otherwise remains in the GitHub promotion workflows. The workflow now explicitly carries dual CI-equivalent gates for review-based promotion loops plus a post-reconcile replay gate: authoritative source/reconcile before review opens, review branch before review closes, authoritative rerun only when replay changes the source codebase, and canonical-branch replay proof before promotion resumes from reconcile-origin packages. |
| `wf-docker-update-ci-pipeline-method` | `already-backed` | Existing support via [`ci_pipeline_surface_audit.sh`](../tools/ci_pipeline_surface_audit.sh); use it to inventory workflow coverage and expected stack hints before/after CI edits. CI-equivalent semantics, broad stage-gate naming, and lifecycle-step parity now delegate to `ci-equivalent-governance`, while stage-facing test-surface rewires route through `ci-equivalent-test-surface-admission`; this workflow remains responsible for the actual pipeline/job edits and for keeping local stage contracts synchronized in the same change. |
| `wf-docker-update-runtime-and-ingress-method` | `already-backed` | Existing support via [`runtime_ingress_surface_audit.sh`](../tools/runtime_ingress_surface_audit.sh); use it to inventory runtime/ingress surfaces and compose readiness before/after runtime edits. |
| `wf-docker-update-skill-method` | `already-backed` | Existing support via [`list_public_codex_skill_mirrors.sh`](../tools/list_public_codex_skill_mirrors.sh), [`sync_cline_skill_mirrors.sh`](../tools/sync_cline_skill_mirrors.sh), [`sync_claude_skill_mirrors.sh`](../tools/sync_claude_skill_mirrors.sh), [`sync_codex_public_skill_mirrors.sh`](../tools/sync_codex_public_skill_mirrors.sh), [`sync_clinerules_mirrors.sh`](../tools/sync_clinerules_mirrors.sh), and [`self_check.sh`](../tools/self_check.sh); use them to keep canonical, `.cline`, `.claude`, `.clinerules`, and tracked public Codex mirrors converged. |

## Flutter Workflow Skills
| Skill | Classification | Support / Preferred Shape |
| --- | --- | --- |
| `wf-flutter-create-controller-method` | `already-backed` | Existing support via [`flutter_workflow_scaffold.sh`](../tools/flutter_workflow_scaffold.sh) with `--kind controller`; use it for the repeatable checklist before controller implementation. |
| `wf-flutter-create-domain-method` | `already-backed` | Existing support via [`flutter_workflow_scaffold.sh`](../tools/flutter_workflow_scaffold.sh) with `--kind domain`; use it for repeatable doc/file/test preparation before aggregate design. |
| `wf-flutter-create-repository-method` | `already-backed` | Existing support via [`flutter_workflow_scaffold.sh`](../tools/flutter_workflow_scaffold.sh) with `--kind repository`; use it for repeatable mapper/contract/test preparation before implementation. |
| `wf-flutter-create-route-method` | `already-backed` | Existing support via [`flutter_route_contract_audit.sh`](../tools/flutter_route_contract_audit.sh); use it after route generation and classify every hit before delivery. |
| `wf-flutter-create-screen-method` | `already-backed` | Existing support via [`flutter_workflow_scaffold.sh`](../tools/flutter_workflow_scaffold.sh) with `--kind screen`; use it for repeatable screen/controller/route checklist preparation. |

## Laravel Workflow Skills
| Skill | Classification | Support / Preferred Shape |
| --- | --- | --- |
| `wf-laravel-create-api-endpoint-method` | `already-backed` | Existing support via [`laravel_workflow_scaffold.sh`](../tools/laravel_workflow_scaffold.sh) with `--kind api-endpoint`; use it to structure contract/route/security/test preparation before edits. |
| `wf-laravel-create-domain-method` | `already-backed` | Existing support via [`laravel_workflow_scaffold.sh`](../tools/laravel_workflow_scaffold.sh) with `--kind domain`; use it to structure docs/model/migration/test preparation before implementation. |
| `wf-laravel-create-package-method` | `already-backed` | Existing support via [`assert_package_decoupling.py`](wf-laravel-create-package-method/scripts/assert_package_decoupling.py); more validators could be added later. |
| `wf-laravel-domain-resolution-testing` | `already-backed` | Existing support via [`laravel_domain_resolution_test_audit.sh`](../tools/laravel_domain_resolution_test_audit.sh); use it to classify touched tests before claiming resolution coverage. |
| `wf-laravel-tenant-access-guardrails` | `already-backed` | Existing support via [`laravel_tenant_access_guardrails_audit.sh`](../tools/laravel_tenant_access_guardrails_audit.sh); use it for static route-file guardrail checks before deeper validation. |
