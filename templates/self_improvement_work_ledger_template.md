# Template: Self-Improvement Work Ledger

Use this file as a temporary, non-authoritative work ledger for longer instruction-only sessions, typically at `delphi-ai/artifacts/tmp/self-improvement-work-ledger.md`.

This ledger exists only to help Delphi manage multi-step self-improvement work inside `delphi-ai/`. It must never become:
- a tactical implementation TODO;
- an approval gate;
- a substitute for canonical instructions;
- a substitute for downstream `foundation_documentation/**` artifacts.

Delete or refresh it when the active self-improvement scope closes. Do not promote it into canonical documentation.

## 0. Artifact Role

- **Session scope:** `instruction-only`
- **Purpose in this session:** `<why this temporary ledger exists right now>`
- **What it is not:** `<tactical TODO|approval ledger|canonical roadmap|downstream artifact>`
- **Closure intent:** `<delete at session closure|refresh only while this self-improvement arc remains open>`

## 1. Current Objective

- `<current objective>`

## 2. Active Workstreams

| ID | Workstream | Current State | Files / Surfaces | Why It Matters Now |
| --- | --- | --- | --- | --- |
| `SI-01` | `<workstream>` | `<planned|active|blocked|done>` | `<path(s)>` | `<why now>` |

## 3. Open Decisions / Questions

| ID | Topic | Current State | Why It Matters | Current Handling | Next Target |
| --- | --- | --- | --- | --- | --- |
| `Q-01` | `<topic>` | `<open|partial|deferred|closed>` | `<impact>` | `<review now|defer|resolved>` | `<next step>` |

## 4. Current Order

1. `<next work item>`

## 5. Current Blockers

- `<blocker or none>`

## 6. Validation Plan

- `<local checks, agnosticism review, sync expectations, or n/a>`

## 7. Next Exact Step

- `<single next action>`
